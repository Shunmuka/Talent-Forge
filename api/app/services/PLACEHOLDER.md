# Services Layer TODO
- Define `analyze_resume(resume: str, job_description: str) -> AnalyzeResponse`.
- Define `rewrite_resume_section(resume: str, job_description: str) -> RewriteResponse`.
- Capture side effects such as storage, telemetry, and retries once finalized.
